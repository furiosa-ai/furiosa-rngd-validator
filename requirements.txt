--extra-index-url https://download.pytorch.org/whl/cpu

furiosa-llm==2026.3.0
# vllm benchmark scripts import PIL via `benchmark_dataset.py`.
pillow
# `rngd_diag_decoder` parses the diag.yaml output from rngd-diag.
pyyaml
# CPU-only builds; +cpu wheels exist only on the PyTorch index above
# (both x86_64 and aarch64). Must match the torch version furiosa-llm
# requires exactly.
torch==2.10.0+cpu
torchvision==0.25.0+cpu
